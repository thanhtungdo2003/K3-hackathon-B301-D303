/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Chế độ trình chiếu chiếu lên máy chiếu nên không được có phù hiệu dev nào.
  devIndicators: false,
};

export default nextConfig;
